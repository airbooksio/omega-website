{
  "name": "airbooks-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Airbooks single-page marketing website — design system and token build for Framer deployment.",
  "scripts": {
    "tokens": "style-dictionary build --config style-dictionary.config.mjs",
    "tokens:watch": "style-dictionary build --config style-dictionary.config.mjs --watch",
    "tokens:clean": "rm -rf dist"
  },
  "devDependencies": {
    "style-dictionary": "^5.0.0"
  }
}
